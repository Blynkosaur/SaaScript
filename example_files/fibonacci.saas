# Fibonacci sequence - recursive approach
mvp fibonacci(n) {
    disrupt (n <= 1) {
        saas n;
    }
    saas fibonacci(n - 1) + fibonacci(n - 2);
}

leverage("First 10 Fibonacci numbers:");
bootstrap i = 0;
b2b (i < 10) {
    bootstrap fib = fibonacci(i);
    leverage(fib);
    i = i + 1;
}
